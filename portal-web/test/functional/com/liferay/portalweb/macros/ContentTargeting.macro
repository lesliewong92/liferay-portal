<definition>
	<command name="enableAnalytics">
		<execute macro="Panel#expandPanel">
			<var name="panelHeading" value="Audience Targeting Analytics" />
		</execute>

		<for list="${analyticsList}" param="analytic">
			<execute function="Portal.Check#checkToggleSwitch" locator1="ToggleSwitch#ANY_TOGGLE_SWITCH">
				<var name="key_toggleSwitchLabel" value="${analytic}" />
			</execute>
		</for>
	</command>

	<command name="expandPreview">
		<var name="key_portletName" value="${portletName}" />

		<if>
			<condition function="Portal.IsElementNotPresent" locator1="ContentTargeting#PREVIEW_BUTTON_EXPANDED" />
			<then>
				<execute function="Portal.Click#makeVisibleClickAt" locator1="Portlet#SPECIFIC_BORDERLESS_BAR" />

				<execute function="Portal.Click" locator1="ContentTargeting#PREVIEW_BUTTON" />

				<execute function="Portal.AssertElementPresent" locator1="ContentTargeting#PREVIEW_BUTTON_EXPANDED" />
			</then>
		</if>
	</command>

	<command name="expandPreviewOnStagingLiveMode">
		<var name="key_portletName" value="${portletName}" />

		<if>
			<condition function="Portal.IsElementNotPresent" locator1="ContentTargeting#PREVIEW_BUTTON_EXPANDED" />
			<then>
				<execute function="Portal.MouseOver" locator1="ContentTargeting#PREVIEW_BUTTON_STAGING_LIVE_MODE" />

				<execute function="Portal.Click" locator1="ContentTargeting#PREVIEW_BUTTON" />

				<execute function="Portal.AssertElementPresent" locator1="ContentTargeting#PREVIEW_BUTTON_EXPANDED" />
			</then>
		</if>
	</command>

	<command name="viewEntryOnStagingLiveMode">
		<var name="key_portletName" value="${portletName}" />

		<execute function="Portal.AssertTextEquals" locator1="ContentTargeting#VIEW_ENTRY_CONTENT" value1="${entryContentText}" />

		<for list="${previewContentList}" param="previewEntry">
			<execute macro="ContentTargeting#expandPreviewOnStagingLiveMode">
				<var name="portletName" value="${portletName}" />
			</execute>

			<var name="key_previewEntry" value="${previewEntry}" />
			<var name="key_viewEntrySelected" value="${viewSelectedContent}" />

			<execute function="Portal.AssertElementPresent" locator1="ContentTargeting#PREVIEW_ENTRY_SELECTED" />
			<execute function="Portal.AssertTextEquals" locator1="ContentTargeting#PREVIEW" value1="${previewEntry}" />
		</for>
	</command>
</definition>